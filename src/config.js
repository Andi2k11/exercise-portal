// Development configuration toggles
// Set `dev.overrideCount.enabled` to true to force generated exercises to use `overrideCount.value`.
export const dev = {
  overrideCount: {
    enabled: false,
    value: 5
  }
};

export default { dev };
